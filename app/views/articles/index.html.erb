<div class="titlebar">
  <div class="toptitle">
    <h2 class="redh2">Les Articles</h2>
    <%= link_to "Créer un nouvel article", new_article_path, class: "button-crud"%>
  </div>
  <%= alphabetical_paginate @alphaParams%>
</div>

<div id="pagination_table">
  <div class="pagecontent">
      <% @articles.each do |art| %>
        <div class="block <%= art.random_color%>">
          <div class="block-margin">
            <span class="block-author"><%= art.author.full_name%></span>
            <span class="block-content"><%= art.fr_name%></span>
            <%= link_to "Lire la suite", art, class: "block-link"%>
          </div>
        </div>
      <%end%>
  </div>
</div>

<br>
